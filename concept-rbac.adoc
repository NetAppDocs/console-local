---
sidebar: sidebar
permalink: concept-rbac.html
keywords: RBAC, role-based access control, roles, permissions, access, least privilege, netapp console local
summary: "Manage user access to NetApp Console Local with role-based access control (RBAC), assigning predefined roles at the organization, folder, or fleet level."
---
= Understand role-based access control (RBAC)
:icons: font
:imagesdir: ./media/

[.lead]
Manage user access to the NetApp Console Local with role-based access control (RBAC), assigning predefined roles at the organization, folder, or fleet level. Each role grants specific permissions that define what actions users can perform within their assigned scope.

The NetApp Console Local designs roles with least-privilege principles, so each role includes only the permissions needed for its tasks. This approach enhances security by limiting access to what each member requires.

After you organize resources into folders and fleets, assign organization members a role or roles for specific folders or fleets that allow them to perform only their responsibilities.

For example, you can assign a member the Ransomware Resilience admin role at a specific fleet level, allowing them to perform Ransomware Resilience operations for resources within that fleet without granting them broader access to the entire organization. This same user can be granted the role for several fleets within your organization.

You can assign users multiple roles for the same scope or different scopes, depending on their responsibilities. For example, a smaller organization might have the same user manage both Ransomware Resilience and Backup and Recovery tasks at the organization level, while a larger organization might have different users assigned to each role at the fleet level.

== Types of Console Local organization members

There are three types of members in a NetApp Console Local organization:

* *User accounts*: Individual users who log in to the NetApp Console Local to manage resources. Users must sign up for the NetApp Console Local before they can be added to an organization.
* *Service accounts*: Non-human accounts used by applications or services to interact with the NetApp Console Local via APIs. You can add service accounts directly to your Console Local organization.
* *Federated groups*: Groups synchronized from your identity provider (IdP) that allow you to manage access for multiple users collectively. Each user within a federated group must have signed up for the NetApp Console Local and been added to your organization with an access role before they can access resources granted to the group.

link:task-users-add.html[Learn how to add members to your organization].

== Predefined roles in NetApp Console Local

The NetApp Console Local includes predefined roles that you can assign to organization members. Each role includes permissions that specify what actions a member can do within their assigned scope (organization, folder, or fleet).

Roles are categorized by the type of access they provide:

* *Platform roles*: Provide Console Local administration permissions, including organization management and user access control.
* *Data services roles*: Provide permissions for managing specific data services, such as Ransomware Resilience and Backup and Recovery.
* *Application roles*: Provide permissions for managing storage as well as audit Console Local events and alerts.

You can assign multiple roles to a member based on their responsibilities. For example, you might assign a member both the Ransomware Resilience admin role and the Backup and Recovery admin role for a specific fleet.
