---
sidebar: sidebar
permalink: task-manage-member-security.html
keywords: member security, password reset, MFA, multi-factor authentication, service account, credentials, netapp console local
summary: "Manage member security settings in NetApp Console Local, including password resets, MFA management, and service account credentials."
---
= Manage member security
:icons: font
:imagesdir: ./media/

[.lead]
Secure user access to your NetApp Console Local organization by managing member security settings. You can reset user passwords, manage multi-factor authentication (MFA), and recreate service account credentials.

.Required access roles
You must have the Organization admin role to manage member security settings. Folder or fleet admins can manage member security only for folders and fleets that they administer. link:reference-iam-predefined-roles.html[Learn about access roles].

== Reset user passwords (local users only)

Organization admins cannot reset user passwords for local users. However, they can instruct users to reset their own passwords.

Instruct a user to reset their password from the Console Local login page by selecting *Forgot password?*.

NOTE: This option is not available for users in a federated organization.

== Manage a user's multi-factor authentication (MFA)

If a user loses access to their MFA device, you can either remove or disable their MFA configuration.

NOTE: Multi-factor authentication is only available for local users. Federated users cannot enable MFA.

Users must set up MFA again when they log in after removal. If the user temporarily loses access to their MFA device, they can use their saved recovery code to log in.

If they do not have their recovery code, temporarily disable MFA to allow login. When you disable MFA for a user, it is disabled for only eight hours and then re-enabled automatically. The user is allowed one login during that time without MFA. After the eight hours, the user must use MFA to log in.

NOTE: To manage a user's multi-factor authentication, you must have an email address in the same domain as the affected user.

.Steps

. Select *Administration* > *Identity and access*.

. Select *Members*.
+
The Members table lists the members of your organization.

. From the Members page, navigate to a member in the table, select image:icon-action.png[An icon with three dots] and then select *Manage multi-factor authentication*.

. Choose whether to remove or to disable the user's MFA configuration.

== Recreate the credentials for a service account

You can create new credentials for a service account if you lose or need to update them.

Creating new credentials deletes the old ones. You cannot use the old credentials.

.Steps

. Select *Administration* > *Identity and access*.

. Select *Members*.

. In the Members table, navigate to a service account, select image:icon-action.png[An icon with three dots] and then select *Recreate secrets*.

. Select *Recreate*.

. Download or copy the client ID and client secret.
+
The Console Local shows the client secret only once. Make sure you copy or download it and store it securely.
