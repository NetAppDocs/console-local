---
sidebar: sidebar
permalink: task-analyzer-volume-capacity-investigate.html
keywords: capacity, snapshot, storage efficiency, forecast, almost full, workload analyzer, netapp console local deployment
summary: "Investigate capacity and snapshot growth on a volume using the Workload Analyzer to understand physical space, storage efficiency, and snapshot reserve trends."
---
= Investigate capacity growth on a volume in NetApp Console (local deployment)
:hardbreaks:
:nofooter:
:icons: font
:linkattrs:
:imagesdir: ./media/

[.lead]
// Jira: SPSPM-3315

Use Workload Analyzer in NetApp Console (local deployment) to investigate why a volume is running out of space.

When a volume is filling up or snapshot copies are consuming more space than expected, examine capacity and snapshot trends over time. The capacity analysis section shows how physical and available space change, breaks down storage efficiency savings, and projects when the volume will reach a capacity threshold.


.Steps

. Select *Health > Workload Analyzer*.

. In the *Volume* field, search for and select the volume you want to investigate.

. Set the *Time Range* to cover the period of capacity growth, then select *Analyze*.

. Scroll to the *Capacity Analysis* section.

. Select *Capacity view* from the *View* dropdown.
+
The chart shows physical capacity as the bottom stacked area and available capacity as the top stacked area. Together they equal the total volume size, so you can see how quickly available space is decreasing.

. Hover over a point on the chart to see the storage efficiency breakdown, including deduplication, compression, and compaction savings, and the overall storage efficiency ratio.
+
A declining efficiency ratio while physical capacity rises can indicate that newly written data isn't deduplicating or compressing as well as the existing data.

. To investigate snapshot consumption, select *Snapshot View* from the *View* dropdown.
+
The chart shows the snapshot reserve as a horizontal reference line and snapshot used capacity as an area below it. Hover to see the snapshot space used and its percentage of the reserve, the total snapshot count, and the age of the oldest snapshot.
+
When snapshot consumption exceeds the reserve, snapshots begin consuming space from the volume's data area, which reduces the space available for new writes.

. If the volume tiers data to the cloud, select *Cloud Tier View* from the *View* dropdown to compare how much capacity is on the local performance tier versus the cloud capacity tier.

. Enable *Show Forecast* to project when the volume will reach a warning or critical capacity threshold.
+
// ENGINEERING REVIEW: Confirm the capacity forecast minimum-data requirement and the "almost full" threshold for Console (local deployment). The following values are carried over from Active IQ Unified Manager and may differ.
The capacity forecast requires at least 10 days of historical data. When fewer than 30 days of capacity remain before the volume is full, the analyzer identifies the storage as almost full. The forecast displays an insight message with an estimated time to breach.

.After you finish

Use the capacity trends you observe to decide what to do next:

* If available capacity is trending toward full, consider increasing the volume size, enabling autogrow, or moving data off the volume.
* If snapshot used capacity is exceeding the reserve, review your snapshot policy and retention to reclaim space.
* If the storage efficiency ratio is dropping, review whether the workload's data type or inline efficiency settings are reducing savings. Use link:reference-workload-analyzer-metrics.html[Workload Analyzer metrics reference] for detailed descriptions of capacity, snapshot, and efficiency metrics.
* If the volume has a capacity-related alert that Console (local deployment) can resolve, the alert may offer a Fix-It option. See link:task-remediation-guided.html[Remediate with guided steps].
